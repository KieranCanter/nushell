# nushell

Most of this is directly from [echasnovski's config](https://github.com/echasnovski/dotfiles/tree/master/nushell/.config/nushell).

## deps

install `libigt2`, `vivid` via package manager
install `nu_plugin_gstat` via cargo and then `plugin add ~/.cargo/bin/nu_plugin_gstat`
